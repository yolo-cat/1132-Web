{
  "name": "${FreeCSS Performance}",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "devDependencies": {
    "@lhci/cli": "^0.14.0",
    "lighthouse": "^10.4.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lighthouse": "lighthouse https://yolo-cat.github.io/1132-WEB-FreeCSS-Performance/ --output html --output-path ./lh-report.html --chrome-flags='--headless'",
    "lhci:collect": "lhci collect",
    "lhci:assert": "lhci assert",
    "lhci": "lhci autorun && node generate-lhci-index.js"
  },
  "private": true
}
